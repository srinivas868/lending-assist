---
applications:
- name: lending-assist-server
  host: lending-assist-server
  memory: 1024M
  instances: 1
  disk_quota: 1024M
  # services:
  # - mean-starter-mongodb
  # env:
  #   NODE_ENV: cloud-foundry
# declared-services:
#   mean-starter-mongodb:
#     label: compose-for-mongodb
#     plan: Standard
