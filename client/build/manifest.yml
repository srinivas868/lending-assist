---
applications:
- name: ibm-lending-assist-dev
  host: lending-assist-dev
  memory: 256M
  instances: 2
  disk_quota: 1024M
  # services:
  # - mean-starter-mongodb
  # env:
  #   NODE_ENV: cloud-foundry
# declared-services:
#   mean-starter-mongodb:
#     label: compose-for-mongodb
#     plan: Standard
